# Data Directory

Repository-managed knowledge base files live in:

- `data/knowledge_base/`

Place HR policy PDFs there before running reindexing.

The lightweight HR SQLite database is stored at:

- `data/hr.sqlite3`

Seed data for the HR database lives in:

- `data/hr_seed.sql`
